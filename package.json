{
  "name": "cake-cli",
  "version": "1.0.0",
  "description": "",
  "main": "dist/index.js",
  "scripts": {
    "babel": "babel --copy-files --out-dir dist src",
    "build": "npm run babel",
    "dev": "npm run babel -- --watch",
    "make-component": "node ./dist/index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "ejs": "^2.5.7",
    "handlebars": "^4.0.10",
    "humps": "^2.0.1",
    "inquirer": "^3.2.1"
  },
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-env": "^1.6.1"
  }
}
